<Window x:Class="CanXe.Desktop.Views.Admin.AdminUnlockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MỞ KHÓA ADMIN"
        Height="220" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource CardBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="MỞ KHÓA ADMIN" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"
                   Foreground="{DynamicResource PrimaryTextBrush}"/>
        <TextBlock Grid.Row="1" Text="Nhập mã Admin" FontSize="14" Margin="0,0,0,6"
                   Foreground="{DynamicResource SecondaryTextBrush}"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Height="36" FontSize="16" KeyDown="PasswordBox_OnKeyDown"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Mở khóa" Width="100" Height="34" Margin="0,0,8,0" IsDefault="True" Click="Unlock_Click"
                    Style="{StaticResource CanXePrimaryButtonStyle}"/>
            <Button Content="Hủy" Width="90" Height="34" IsCancel="True" Click="Cancel_Click"
                    Style="{StaticResource CanXeSecondaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
