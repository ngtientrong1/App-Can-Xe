<Window x:Class="CanXe.Desktop.Views.Admin.ManualWeighWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NHẬP CÂN THỦ CÔNG"
        MinWidth="440"
        Width="440"
        MinHeight="360"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource CardBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <Border Padding="24,20,24,24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="NHẬP CÂN THỦ CÔNG" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"
                       Foreground="{DynamicResource PrimaryTextBrush}"/>
            <TextBlock Grid.Row="1" Text="Cân lần:" FontSize="14" Margin="0,0,0,6"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,14">
                <RadioButton x:Name="Sequence1Radio" Content="Lần 1" Margin="0,0,16,0"/>
                <RadioButton x:Name="Sequence2Radio" Content="Lần 2"/>
            </StackPanel>
            <TextBlock Grid.Row="3" Text="Trọng lượng kg:" FontSize="14" Margin="0,0,0,6"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>
            <TextBox x:Name="WeightTextBox" Grid.Row="4" MinHeight="44" FontSize="16" Margin="0,0,0,14"
                     Style="{StaticResource CanXeTextBoxStyle}"/>
            <TextBlock Grid.Row="5" Text="Lý do:" FontSize="14" Margin="0,0,0,6"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>
            <TextBox x:Name="ReasonTextBox" Grid.Row="6" MinHeight="64" FontSize="15" Margin="0,0,0,8"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     Style="{StaticResource CanXeTextBoxStyle}"/>
            <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Xác nhận" MinWidth="120" MinHeight="44" Margin="0,0,10,0" Padding="16,8"
                        IsDefault="True" Click="Confirm_Click"
                        Style="{StaticResource CanXePrimaryButtonStyle}"/>
                <Button Content="Hủy" MinWidth="100" MinHeight="44" Padding="16,8"
                        IsCancel="True" Click="Cancel_Click"
                        Style="{StaticResource CanXeSecondaryButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
