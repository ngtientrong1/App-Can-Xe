<Window x:Class="CanXe.Desktop.Views.Admin.ManualWeighWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NHẬP CÂN THỦ CÔNG"
        Height="320" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="NHẬP CÂN THỦ CÔNG" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
        <TextBlock Grid.Row="1" Text="Cân lần:" FontSize="14" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <RadioButton x:Name="Sequence1Radio" Content="Lần 1" Margin="0,0,16,0"/>
            <RadioButton x:Name="Sequence2Radio" Content="Lần 2"/>
        </StackPanel>
        <TextBlock Grid.Row="3" Text="Trọng lượng kg:" FontSize="14" Margin="0,0,0,4"/>
        <TextBox x:Name="WeightTextBox" Grid.Row="4" Height="36" FontSize="16" Margin="0,0,0,12"/>
        <TextBlock Grid.Row="5" Text="Lý do:" FontSize="14" Margin="0,0,0,4"/>
        <TextBox x:Name="ReasonTextBox" Grid.Row="6" Height="56" FontSize="15" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Xác nhận" Width="100" Height="34" Margin="0,0,8,0" IsDefault="True" Click="Confirm_Click"/>
            <Button Content="Hủy" Width="90" Height="34" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
