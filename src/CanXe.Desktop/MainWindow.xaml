<Window x:Class="CanXe.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:CanXe.Desktop.Converters"
        mc:Ignorable="d"
        x:Name="RootWindow"
        Title="CanXe — Cân xe"
        MinWidth="1280"
        MinHeight="720"
        Width="1536"
        Height="864"
        WindowStartupLocation="CenterScreen"
        Background="White">
    <Window.Resources>
        <conv:MaxWidthFractionConverter x:Key="MaxWidthFractionConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" MinHeight="280"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="240"/>
        </Grid.RowDefinitions>

        <!-- TOP: Live weight | Ticket form | Camera -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*" MinWidth="200"/>
                <ColumnDefinition Width="2.2*" MinWidth="420"/>
                <ColumnDefinition Width="0.9*" MinWidth="180"/>
            </Grid.ColumnDefinitions>

            <!-- Live weight -->
            <Border Grid.Column="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="12" Margin="0,0,6,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="TRỌNG LƯỢNG TRỰC TIẾP" Style="{StaticResource SectionHeader}"/>
                    <Viewbox Stretch="Uniform" MaxHeight="120" HorizontalAlignment="Left">
                        <TextBlock Text="{Binding LiveWeightKg, StringFormat={}{0:N0}}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                    </Viewbox>
                    <TextBlock Text="kg" FontSize="20" Foreground="#616161"/>
                </StackPanel>
            </Border>

            <!-- Ticket info -->
            <Border Grid.Column="1" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="12" Margin="3,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="Số phiếu" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Text="{Binding DisplayNumber}" FontSize="18" FontWeight="SemiBold"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <TextBlock Text="Ngày giờ" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Text="{Binding TicketDateTimeDisplay}" FontSize="16"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="Tên khách hàng" Style="{StaticResource SectionHeader}"/>
                        <TextBox Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}"/>
                        <ListBox ItemsSource="{Binding CustomerSuggestions}" MaxHeight="80"
                                 PreviewMouseLeftButtonUp="SuggestionList_OnMouseUp" Tag="Customer"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="1">
                        <TextBlock Text="Biển số xe" Style="{StaticResource SectionHeader}"/>
                        <TextBox Text="{Binding LicensePlate, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="Loại hàng" Style="{StaticResource SectionHeader}"/>
                        <TextBox Text="{Binding CargoTypeName, UpdateSourceTrigger=PropertyChanged}"/>
                        <ListBox ItemsSource="{Binding CargoTypeSuggestions}" MaxHeight="80"
                                 PreviewMouseLeftButtonUp="SuggestionList_OnMouseUp" Tag="CargoType"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Grid.Column="1">
                        <TextBlock Text="Đơn giá (VNĐ/kg)" Style="{StaticResource SectionHeader}"/>
                        <TextBox Text="{Binding UnitPriceText, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.ColumnSpan="2">
                        <TextBlock Text="Ghi chú" Style="{StaticResource SectionHeader}"/>
                        <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <UniformGrid Grid.Row="4" Grid.ColumnSpan="2" Columns="4" Margin="0,6,0,0">
                        <StackPanel Margin="0,0,4,0">
                            <TextBlock Text="Lần 1 (kg)" Style="{StaticResource SectionHeader}"/>
                            <TextBlock Text="{Binding Weight1Display}" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Margin="4,0">
                            <TextBlock Text="Lần 2 (kg)" Style="{StaticResource SectionHeader}"/>
                            <TextBlock Text="{Binding Weight2Display}" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Margin="4,0">
                            <TextBlock Text="Tổng / Bì / Hàng" Style="{StaticResource SectionHeader}"/>
                            <TextBlock FontWeight="SemiBold">
                                <Run Text="{Binding GrossDisplay, Mode=OneWay}"/>
                                <Run Text=" / "/>
                                <Run Text="{Binding TareDisplay, Mode=OneWay}"/>
                                <Run Text=" / "/>
                                <Run Text="{Binding NetDisplay, Mode=OneWay}"/>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Margin="4,0,0,0">
                            <TextBlock Text="KL TT / Thành tiền" Style="{StaticResource SectionHeader}"/>
                            <TextBlock FontWeight="SemiBold">
                                <Run Text="{Binding BillableDisplay, Mode=OneWay}"/>
                                <Run Text=" / "/>
                                <Run Text="{Binding TotalAmountDisplay, Mode=OneWay}"/>
                            </TextBlock>
                        </StackPanel>
                    </UniformGrid>

                    <TextBlock Grid.Row="5" Grid.ColumnSpan="2" Margin="0,4,0,0" Foreground="#757575" FontSize="12"
                               Text="{Binding DeductionDisplay, StringFormat=Khấu trừ: {0} kg}"/>
                </Grid>
            </Border>

            <!-- Camera + dev sim -->
            <Border Grid.Column="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="8" Margin="6,0,0,0"
                    MaxWidth="{Binding ElementName=RootWindow, Path=ActualWidth, Converter={StaticResource MaxWidthFractionConverter}, ConverterParameter=0.2}">
                <StackPanel>
                    <TextBlock Text="CAMERA (MÔ PHỎNG)" Style="{StaticResource SectionHeader}"/>
                    <Border Height="100" Background="#263238" Margin="0,0,0,8">
                        <TextBlock Text="Preview" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="DEV — CÂN MÔ PHỎNG" Style="{StaticResource SectionHeader}"/>
                    <CheckBox Content="Chế độ thủ công" IsChecked="{Binding IsManualScaleMode}" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding ManualWeightText, UpdateSourceTrigger=PropertyChanged}" ToolTip="Nhập kg thủ công"/>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <Button Content="8.500" Command="{Binding SetManualScale8500Command}" Margin="0,0,4,0" Padding="8,4"/>
                        <Button Content="18.500" Command="{Binding SetManualScale18500Command}" Padding="8,4"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Action buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4">
            <Button Content="GHI TRỌNG LƯỢNG" Style="{StaticResource RecordWeightButton}"
                    Command="{Binding RecordWeightCommand}"
                    IsEnabled="{Binding IsRecordWeightEnabled}"/>
            <Button Content="LƯU" Style="{StaticResource ActionButton}" Command="{Binding SaveCommand}"/>
            <Button Content="HỦY BỎ" Style="{StaticResource ActionButton}" Command="{Binding CancelCommand}" Background="#616161"/>
        </StackPanel>

        <!-- Status bar -->
        <Border Grid.Row="2" Background="#EEEEEE" Padding="8,4" Margin="0,4">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#424242"/>
        </Border>

        <!-- Bottom: filters + list -->
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <WrapPanel Grid.Row="0" Margin="0,0,0,6">
                <StackPanel Orientation="Horizontal" Margin="0,0,8,4">
                    <TextBlock Text="Từ ngày" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <DatePicker SelectedDate="{Binding FilterFromDate}" Width="120"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,8,4">
                    <TextBlock Text="Đến ngày" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <DatePicker SelectedDate="{Binding FilterToDate}" Width="120"/>
                </StackPanel>
                <TextBox Width="100" Margin="0,0,4,4" ToolTip="Khách hàng" Text="{Binding FilterCustomerName, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBox Width="100" Margin="0,0,4,4" ToolTip="Loại hàng" Text="{Binding FilterCargoTypeName, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBox Width="90" Margin="0,0,4,4" ToolTip="Biển số" Text="{Binding FilterLicensePlate, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBox Width="80" Margin="0,0,4,4" ToolTip="Số phiếu" Text="{Binding FilterDisplayNumber, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBox Width="80" Margin="0,0,4,4" ToolTip="Đơn giá" Text="{Binding FilterUnitPriceText, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Lọc" Command="{Binding ApplyFilterCommand}" Margin="0,0,4,4" Padding="10,4"/>
                <Button Content="Hôm nay" Command="{Binding FilterTodayCommand}" Margin="0,0,4,4" Padding="10,4"/>
                <Button Content="Xóa lọc" Command="{Binding ClearFilterCommand}" Margin="0,0,4,4" Padding="10,4"/>
            </WrapPanel>

            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Tickets}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#FAFAFA"
                      MouseDoubleClick="TicketsGrid_OnMouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ngày giờ" Binding="{Binding TicketDateTime, StringFormat=dd/MM/yyyy HH:mm}" Width="130"/>
                    <DataGridTextColumn Header="Số phiếu" Binding="{Binding DisplayNumber}" Width="80"/>
                    <DataGridTextColumn Header="Biển số" Binding="{Binding LicensePlate}" Width="90"/>
                    <DataGridTextColumn Header="Khách hàng" Binding="{Binding CustomerName}" Width="120"/>
                    <DataGridTextColumn Header="Loại hàng" Binding="{Binding CargoTypeName}" Width="100"/>
                    <DataGridTextColumn Header="Lần 1" Binding="{Binding Weight1Kg, StringFormat=N0}" Width="70"/>
                    <DataGridTextColumn Header="Lần 2" Binding="{Binding Weight2Kg, StringFormat=N0}" Width="70"/>
                    <DataGridTextColumn Header="Tổng" Binding="{Binding GrossWeightKg, StringFormat=N0}" Width="70"/>
                    <DataGridTextColumn Header="Bì" Binding="{Binding TareWeightKg, StringFormat=N0}" Width="60"/>
                    <DataGridTextColumn Header="Hàng" Binding="{Binding NetWeightKg, StringFormat=N0}" Width="70"/>
                    <DataGridTextColumn Header="KL TT" Binding="{Binding BillableWeightKg, StringFormat=N0}" Width="70"/>
                    <DataGridTextColumn Header="Đơn giá" Binding="{Binding UnitPriceVndPerKg, StringFormat=N0}" Width="80"/>
                    <DataGridTextColumn Header="Thành tiền" Binding="{Binding TotalAmountVnd, StringFormat=N0}" Width="100"/>
                    <DataGridTextColumn Header="Ghi chú" Binding="{Binding Notes}" Width="*" MinWidth="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
