<Window x:Class="CanXe.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:conv="clr-namespace:CanXe.Desktop.Converters"
        xmlns:ctrl="clr-namespace:CanXe.Desktop.Controls"
        mc:Ignorable="d"
        x:Name="RootWindow"
        Title="CanXe — Cân xe"
        MinWidth="1280"
        MinHeight="720"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        Background="White">
    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:NullToVisibilityConverter x:Key="NullToVis"/>
        <Style x:Key="RightAlignedCell" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="240"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="160"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title + status -->
        <DockPanel Grid.Row="0" Margin="0,0,0,6">
            <TextBlock Text="CanXe — Cân xe" FontSize="20" FontWeight="SemiBold" DockPanel.Dock="Left"/>
            <TextBlock Text="{Binding StatusMessage}" FontSize="16" Foreground="#424242"
                       DockPanel.Dock="Right" TextTrimming="CharacterEllipsis"/>
        </DockPanel>

        <!-- TOP WORK AREA -->
        <Grid Grid.Row="1" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{Binding WeighColumnWidth}" MinWidth="280"/>
                <ColumnDefinition Width="{Binding InfoColumnWidth}" MinWidth="400"/>
                <ColumnDefinition Width="{Binding CameraColumnWidth}" MinWidth="{Binding CameraColumnMinWidth}"/>
            </Grid.ColumnDefinitions>

            <!-- Left: live weight -->
            <Border Grid.Column="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" Padding="10" Margin="0,0,6,0" CornerRadius="6">
                <StackPanel>
                    <TextBlock Text="TRỌNG LƯỢNG TRỰC TIẾP" Style="{StaticResource SectionHeader}" Margin="0,0,0,2"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                        <TextBlock Text="{Binding LiveWeightKg, StringFormat={}{0:N0}}"
                                   FontSize="56" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                        <TextBlock Text=" kg" FontSize="22" Foreground="#616161" VerticalAlignment="Bottom" Margin="0,0,0,8"/>
                    </StackPanel>
                    <TextBlock Text="{Binding ScaleStabilityText}" FontSize="15" Foreground="#2E7D32" Margin="0,0,0,8"/>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" BorderBrush="#BDBDBD" BorderThickness="1" Padding="6" Margin="0,0,4,0"
                                Background="White" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="CÂN LẦN 1" FontWeight="SemiBold" FontSize="15"/>
                                <TextBlock Text="{Binding Weight1ValueText}" FontSize="19" FontWeight="SemiBold" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding Weight1TimeText}" FontSize="13" Foreground="#616161"
                                           Visibility="{Binding Weight1HasValue, Converter={StaticResource BoolToVis}}"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" BorderBrush="#BDBDBD" BorderThickness="1" Padding="6" Margin="4,0,0,0"
                                Background="White" CornerRadius="4">
                            <StackPanel>
                                <TextBlock Text="CÂN LẦN 2" FontWeight="SemiBold" FontSize="15"/>
                                <TextBlock Text="{Binding Weight2ValueText}" FontSize="19" FontWeight="SemiBold" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding Weight2TimeText}" FontSize="13" Foreground="#616161"
                                           Visibility="{Binding Weight2HasValue, Converter={StaticResource BoolToVis}}"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <UniformGrid Columns="3" Margin="0,0,0,4">
                        <StackPanel Margin="0,0,4,0">
                            <TextBlock Text="TỔNG" FontSize="15"/>
                            <TextBlock Text="{Binding GrossDisplay}" FontSize="21" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Margin="4,0">
                            <TextBlock Text="BÌ" FontSize="15"/>
                            <TextBlock Text="{Binding TareDisplay}" FontSize="21" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Margin="4,0,0,0">
                            <TextBlock Text="HÀNG" FontSize="15" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding NetDisplay}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource NetHighlightBrush}"/>
                        </StackPanel>
                    </UniformGrid>

                    <TextBlock FontSize="15" Margin="0,1,0,0">
                        <Run Text="Trừ bì: "/>
                        <Run Text="{Binding DeductionDisplay, Mode=OneWay}"/>
                    </TextBlock>
                    <TextBlock FontSize="15" Margin="0,1,0,0">
                        <Run Text="KL tính tiền: "/>
                        <Run Text="{Binding BillableDisplay, Mode=OneWay}"/>
                    </TextBlock>
                    <TextBlock FontSize="15" Margin="0,1,0,0">
                        <Run Text="Thành tiền: "/>
                        <Run Text="{Binding TotalAmountDisplay, Mode=OneWay}"/>
                    </TextBlock>
                    <TextBlock Text="Cân dịch vụ — chưa nhập đơn giá" Foreground="#757575" FontSize="13" Margin="0,4,0,0"
                               Visibility="{Binding IsServiceWeighVisible, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>
            </Border>

            <!-- Middle: ticket info -->
            <Border Grid.Column="1" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" Padding="12" Margin="3,0" CornerRadius="6">
                <StackPanel VerticalAlignment="Top">
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" Grid.Column="0">
                            <TextBlock Text="Số phiếu:" Style="{StaticResource SectionHeader}" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding DisplayNumber}" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Text="Số dự kiến" Foreground="#757575" FontSize="13" Margin="8,4,0,0"
                                       Visibility="{Binding IsPreviewDisplayNumber, Converter={StaticResource BoolToVis}}"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{Binding TicketDateTimeDisplay}" FontSize="18" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="Khách hàng" Style="{StaticResource SectionHeader}"/>
                            <ctrl:AutoCompleteTextBox x:Name="CustomerField" TabIndex="1"
                                                      Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}"
                                                      ItemsSource="{Binding CustomerSuggestions}" FontSize="19"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Biển số xe" Style="{StaticResource SectionHeader}"/>
                            <ctrl:AutoCompleteTextBox x:Name="VehicleField" TabIndex="2"
                                                      Text="{Binding LicensePlate, UpdateSourceTrigger=PropertyChanged}"
                                                      ItemsSource="{Binding VehicleSuggestions}" FontSize="19"/>
                        </StackPanel>
                    </Grid>

                    <Border x:Name="VehicleContextCard"
                            Background="#E8F5E9" BorderBrush="#A5D6A7" BorderThickness="1"
                            Padding="10" Margin="0,0,0,8" CornerRadius="6"
                            Visibility="{Binding IsVehicleContextVisible, Converter={StaticResource BoolToVis}}"
                            Focusable="True" KeyDown="VehicleContextCard_OnKeyDown">
                        <StackPanel>
                            <TextBlock Text="XE ĐÃ TỪNG CÂN" FontWeight="SemiBold" FontSize="14" Foreground="#2E7D32" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding VehicleContextCustomerText}" FontSize="15"/>
                            <TextBlock Text="{Binding VehicleContextFrequentCargoText}" FontSize="15"/>
                            <TextBlock FontSize="14" Foreground="#616161" Margin="0,2,0,4">
                                <Run Text="Lần gần nhất: "/>
                                <Run Text="{Binding VehicleContextLastUsedText, Mode=OneWay}"/>
                            </TextBlock>
                            <TextBlock Text="{Binding VehicleContextCustomerPreview}" FontSize="13" Foreground="#E65100"
                                       Visibility="{Binding VehicleContextCustomerPreview, Converter={StaticResource NullToVis}}"/>
                            <TextBlock Text="{Binding VehicleContextCargoPreview}" FontSize="13" Foreground="#E65100"
                                       Visibility="{Binding VehicleContextCargoPreview, Converter={StaticResource NullToVis}}"/>
                            <WrapPanel Margin="0,6,0,0">
                                <Button x:Name="ApplyBothButton" Content="DÙNG CẢ HAI"
                                        Command="{Binding ApplyVehicleContextBothCommand}" TabIndex="-1">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource FilterButton}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsApplyBothPrimary}" Value="True">
                                                    <Setter Property="Background" Value="#2E7D32"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Content="CHỈ DÙNG KHÁCH" Command="{Binding ApplyVehicleContextCustomerCommand}"
                                        Style="{StaticResource FilterButton}" TabIndex="-1"/>
                                <Button Content="CHỈ DÙNG LOẠI HÀNG" Command="{Binding ApplyVehicleContextCargoCommand}"
                                        Style="{StaticResource FilterButton}" TabIndex="-1"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="Loại hàng" Style="{StaticResource SectionHeader}"/>
                            <ctrl:AutoCompleteTextBox x:Name="CargoField" TabIndex="3"
                                                      Text="{Binding CargoTypeName, UpdateSourceTrigger=PropertyChanged}"
                                                      ItemsSource="{Binding CargoTypeSuggestions}" FontSize="19"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Đơn giá (VNĐ/kg)" Style="{StaticResource SectionHeader}"/>
                            <TextBox x:Name="UnitPriceField" TabIndex="4"
                                     Text="{Binding UnitPriceText, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel>
                        <TextBlock Text="Ghi chú" Style="{StaticResource SectionHeader}"/>
                        <ctrl:AutoCompleteTextBox x:Name="NotesField" TabIndex="5"
                                                  Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                                                  ItemsSource="{Binding NotesSuggestions}" FontSize="19"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Right: camera -->
            <Border Grid.Column="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" Padding="8" Margin="6,0,0,0" CornerRadius="6"
                    Visibility="{Binding IsCameraPanelVisible, Converter={StaticResource BoolToVis}}">
                <StackPanel>
                    <DockPanel Margin="0,0,0,4">
                        <TextBlock Text="CAMERA" Style="{StaticResource SectionHeader}" DockPanel.Dock="Left"/>
                        <Button Content="THU GỌN" Command="{Binding ToggleCameraPanelCommand}" DockPanel.Dock="Right"
                                Style="{StaticResource FilterButton}" Padding="6,2" TabIndex="-1"/>
                    </DockPanel>
                    <Viewbox Stretch="Uniform" MaxHeight="120" Margin="0,0,0,6">
                        <Grid Width="160" Height="90">
                            <Border Background="#263238" CornerRadius="4">
                                <TextBlock Text="Preview 16:9" Foreground="White" HorizontalAlignment="Center"
                                           VerticalAlignment="Center" FontSize="13"/>
                            </Border>
                        </Grid>
                    </Viewbox>

                    <StackPanel Visibility="{Binding IsDeveloperPanelAvailable, Converter={StaticResource BoolToVis}}"
                                Focusable="False">
                        <Button Content="MỞ MÔ PHỎNG" Command="{Binding ToggleDevPanelCommand}"
                                Style="{StaticResource FilterButton}" TabIndex="-1" Margin="0,0,0,4"
                                Visibility="{Binding IsDevPanelExpanded, Converter={StaticResource BoolToVis}, ConverterParameter=invert}"/>
                        <Border Visibility="{Binding IsDevPanelExpanded, Converter={StaticResource BoolToVis}}"
                                Background="White" BorderBrush="#BDBDBD" BorderThickness="1" Padding="8" CornerRadius="4"
                                Focusable="False">
                            <StackPanel Focusable="False">
                                <DockPanel Margin="0,0,0,4">
                                    <TextBlock Text="DEV — CÂN MÔ PHỎNG" Style="{StaticResource SectionHeader}" DockPanel.Dock="Left"/>
                                    <Button Content="Thu gọn" Command="{Binding ToggleDevPanelCommand}" DockPanel.Dock="Right"
                                            Style="{StaticResource FilterButton}" Padding="6,2" TabIndex="-1"/>
                                </DockPanel>
                                <CheckBox Content="Chế độ thủ công" IsChecked="{Binding IsManualScaleMode}" FontSize="14"
                                          Margin="0,0,0,4" Focusable="False" TabIndex="-1"/>
                                <CheckBox Content="Cho phép sửa lại Cân lần 1" IsChecked="{Binding DeveloperWeight1OverrideEnabled}"
                                          FontSize="13" Margin="0,0,0,4" Focusable="False" TabIndex="-1"/>
                                <TextBlock Text="DEV: đang mở khóa Cân lần 1" Foreground="#E65100" FontSize="12" Margin="0,0,0,4"
                                           Visibility="{Binding IsWeight1DevOverrideWarningVisible, Converter={StaticResource BoolToVis}}"/>
                                <TextBox Text="{Binding ManualWeightText, UpdateSourceTrigger=PropertyChanged}"
                                         Focusable="False" TabIndex="-1"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0" Focusable="False">
                                    <Button Content="8.500" Command="{Binding SetManualScale8500Command}" Style="{StaticResource FilterButton}" TabIndex="-1"/>
                                    <Button Content="18.500" Command="{Binding SetManualScale18500Command}" Style="{StaticResource FilterButton}" TabIndex="-1"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Action buttons -->
        <WrapPanel Grid.Row="2" HorizontalAlignment="Stretch" Margin="0,2">
            <Button x:Name="Weigh1Button" TabIndex="6" Content="{Binding Weigh1ButtonText}" Style="{StaticResource RecordWeightButton}"
                    Command="{Binding CaptureWeight1Command}" IsEnabled="{Binding IsWeigh1Enabled}"/>
            <Button x:Name="Weigh2Button" TabIndex="7" Content="{Binding Weigh2ButtonText}" Style="{StaticResource RecordWeightButton}"
                    Command="{Binding CaptureWeight2Command}" IsEnabled="{Binding IsWeigh2Enabled}"/>
            <Button TabIndex="8" Content="LƯU" Style="{StaticResource SaveButton}" Command="{Binding SaveCommand}"/>
            <Button TabIndex="9" Content="HỦY BỎ" Style="{StaticResource CancelButton}" Command="{Binding CancelCommand}"/>
            <Button TabIndex="10" Content="IN PHIẾU" Style="{StaticResource ActionButton}" Command="{Binding PrintTicketCommand}" Background="#455A64"/>
            <Button Content="MỞ CAMERA" Style="{StaticResource FilterButton}" Command="{Binding ToggleCameraPanelCommand}"
                    TabIndex="-1" Visibility="{Binding IsCameraPanelVisible, Converter={StaticResource BoolToVis}, ConverterParameter=invert}"/>
        </WrapPanel>

        <!-- Filters -->
        <StackPanel Grid.Row="3" Margin="0,4,0,4">
            <WrapPanel Margin="0,0,0,4">
                <Button Content="HÔM NAY" Style="{StaticResource FilterButton}" Command="{Binding FilterTodayCommand}" TabIndex="-1"/>
                <Button Content="HÔM QUA" Style="{StaticResource FilterButton}" Command="{Binding FilterYesterdayCommand}" TabIndex="-1"/>
                <Button Content="7 NGÀY" Style="{StaticResource FilterButton}" Command="{Binding FilterLast7DaysCommand}" TabIndex="-1"/>
                <Button Content="THÁNG NÀY" Style="{StaticResource FilterButton}" Command="{Binding FilterThisMonthCommand}" TabIndex="-1"/>
                <Button Content="{Binding AdvancedFilterToggleText}" Style="{StaticResource FilterButton}"
                        Command="{Binding ToggleAdvancedFilterCommand}" TabIndex="-1" Margin="8,0,0,0"/>
            </WrapPanel>

            <StackPanel Visibility="{Binding IsAdvancedFilterVisible, Converter={StaticResource BoolToVis}}">
                <WrapPanel Margin="0,0,0,4">
                    <StackPanel Orientation="Horizontal" Margin="0,0,12,6">
                        <TextBlock Text="Từ ngày" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="15" FontWeight="SemiBold"/>
                        <DatePicker SelectedDate="{Binding FilterFromDate}" Width="130" FontSize="15" TabIndex="-1"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,12,6">
                        <TextBlock Text="Đến ngày" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="15" FontWeight="SemiBold"/>
                        <DatePicker SelectedDate="{Binding FilterToDate}" Width="130" FontSize="15" TabIndex="-1"/>
                    </StackPanel>
                </WrapPanel>
                <WrapPanel Margin="0,0,0,4">
                    <StackPanel Width="150" Margin="0,0,8,6">
                        <TextBlock Text="Khách hàng" FontSize="15" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding FilterCustomerName, UpdateSourceTrigger=PropertyChanged}" TabIndex="-1"/>
                    </StackPanel>
                    <StackPanel Width="130" Margin="0,0,8,6">
                        <TextBlock Text="Loại hàng" FontSize="15" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding FilterCargoTypeName, UpdateSourceTrigger=PropertyChanged}" TabIndex="-1"/>
                    </StackPanel>
                    <StackPanel Width="120" Margin="0,0,8,6">
                        <TextBlock Text="Biển số" FontSize="15" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding FilterLicensePlate, UpdateSourceTrigger=PropertyChanged}" TabIndex="-1"/>
                    </StackPanel>
                    <StackPanel Width="100" Margin="0,0,8,6">
                        <TextBlock Text="Số phiếu" FontSize="15" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding FilterDisplayNumber, UpdateSourceTrigger=PropertyChanged}" TabIndex="-1"/>
                    </StackPanel>
                    <StackPanel Width="90" Margin="0,0,8,6">
                        <TextBlock Text="Từ giá" FontSize="15" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding FilterUnitPriceText, UpdateSourceTrigger=PropertyChanged}" TabIndex="-1"/>
                    </StackPanel>
                    <StackPanel Width="90" Margin="0,0,8,6">
                        <TextBlock Text="Đến giá" FontSize="15" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding FilterUnitPriceToText, UpdateSourceTrigger=PropertyChanged}" TabIndex="-1"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,6">
                        <Button Content="ÁP DỤNG LỌC" Style="{StaticResource FilterButton}" Command="{Binding ApplyFilterCommand}" TabIndex="-1"/>
                        <Button Content="XÓA LỌC" Style="{StaticResource FilterButton}" Command="{Binding ClearFilterCommand}" TabIndex="-1"/>
                        <Button Content="XUẤT EXCEL" Style="{StaticResource FilterButton}" Command="{Binding ExportExcelStubCommand}" TabIndex="-1" IsEnabled="False"/>
                    </StackPanel>
                </WrapPanel>
            </StackPanel>

            <ItemsControl ItemsSource="{Binding ActiveFilterChips}" Margin="0,0,0,4">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Label}" Margin="0,0,6,4" Padding="8,4" TabIndex="-1"
                                Command="{Binding DataContext.RemoveFilterChipCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="TicketsGrid" Grid.Row="4"
                  ItemsSource="{Binding Tickets}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#FAFAFA"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  HeadersVisibility="Column"
                  CanUserSortColumns="True"
                  TabIndex="-1"
                  MouseDoubleClick="TicketsGrid_OnMouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ngày giờ" Binding="{Binding TicketDateTime, StringFormat=dd/MM/yyyy HH:mm}" Width="130" SortMemberPath="TicketDateTime"/>
                <DataGridTextColumn Header="Số phiếu" Binding="{Binding DisplayNumber}" Width="85" SortMemberPath="DisplayNumber"/>
                <DataGridTextColumn Header="Biển số" Binding="{Binding LicensePlate, TargetNullValue=—}" Width="95"/>
                <DataGridTextColumn Header="Khách hàng" Binding="{Binding CustomerName, TargetNullValue=—}" Width="150"/>
                <DataGridTextColumn Header="Loại hàng" Binding="{Binding CargoTypeName, TargetNullValue=—}" Width="120"/>
                <DataGridTextColumn Header="Tổng" Binding="{Binding GrossWeightKg, StringFormat=N0, TargetNullValue=—}" Width="72"
                                    ElementStyle="{StaticResource RightAlignedCell}"/>
                <DataGridTextColumn Header="Bì" Binding="{Binding TareWeightKg, StringFormat=N0, TargetNullValue=—}" Width="68"
                                    ElementStyle="{StaticResource RightAlignedCell}"/>
                <DataGridTextColumn Header="Hàng" Binding="{Binding NetWeightKg, StringFormat=N0, TargetNullValue=—}" Width="72"
                                    ElementStyle="{StaticResource RightAlignedCell}"/>
                <DataGridTextColumn Header="KL tính tiền" Binding="{Binding BillableWeightKg, StringFormat=N0, TargetNullValue=—}" Width="95"
                                    ElementStyle="{StaticResource RightAlignedCell}"/>
                <DataGridTextColumn Header="Đơn giá" Binding="{Binding UnitPriceVndPerKg, StringFormat=N0, TargetNullValue=—}" Width="85"
                                    ElementStyle="{StaticResource RightAlignedCell}"/>
                <DataGridTextColumn Header="Thành tiền" Binding="{Binding TotalAmountVnd, StringFormat=N0, TargetNullValue=—}" Width="100"
                                    ElementStyle="{StaticResource RightAlignedCell}"/>
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding Notes, TargetNullValue=—}" Width="*" MinWidth="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer summary -->
        <Border Grid.Row="5" Background="#ECEFF1" BorderBrush="#CFD8DC" BorderThickness="1"
                Padding="14,10" Margin="0,6,0,0" CornerRadius="6">
            <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="18" FontWeight="SemiBold" Margin="0,0,24,0">
                    <Run Text="Số phiếu: "/>
                    <Run Text="{Binding FooterTicketCount, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock FontSize="18" FontWeight="SemiBold" Margin="0,0,24,0">
                    <Run Text="Tổng hàng: "/>
                    <Run Text="{Binding FooterTotalNet, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock FontSize="18" FontWeight="SemiBold">
                    <Run Text="Tổng thành tiền: "/>
                    <Run Text="{Binding FooterTotalAmount, Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</Window>
